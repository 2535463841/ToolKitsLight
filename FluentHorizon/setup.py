from setuptools import setup, find_packages


setup(name='fluenthorizon',
      version='1.0',
      description='Horizon for Opensack using flask and vue',
      author='zbw',
      include_package_data=True,
      packages=find_packages())
